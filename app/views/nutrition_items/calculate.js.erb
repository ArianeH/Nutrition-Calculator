$('#result-partial').html("<%= escape_javascript(render partial: 'nutrition-calculation', locals: { selected: @selected, sum_selected: @sum_selected }) %>");

//u can comment these
$('.calculate-link')[0].firstChild.getAttribute("href")
$('.calculate-link')[0].firstChild.getAttribute("href").replace("sum_selected=[^\d+$]","sum_selected=9")
var href = $('.calculate-link').getAttribute("href")


//this one does something
$('.calculate-link').html("<%= escape_javascript(link_to 'Medium sized Apple', calculate_path(item: 'apple-100', sum_selected: @sum_selected), remote: true, class: 'select-btn one-select') %>");
